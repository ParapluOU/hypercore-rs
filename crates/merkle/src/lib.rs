//! `merkle` — BLAKE3 tree, range & inclusion proofs.
//!
//! The verified-storage primitive: hash a sequence of byte blocks into a tree
//! and produce/verify proofs for ranges, so peers can incrementally verify
//! parts of a log without trusting the sender.
//!
//! Scaffold only — no types yet.
